{
  "name": "koridor-worker",
  "description": "Koridor's server: rating, the weekly leaderboard and room expiry, on a scheduled Cloudflare Worker",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "npm run build && node --test lib/elo.test.js",
    "test:e2e": "npm run build && firebase emulators:exec --config ../firebase.json --only database --project demo-koridor \"node lib/sweep.e2e.js\"",
    "dev": "wrangler dev --test-scheduled",
    "tail": "wrangler tail",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260804.1",
    "@types/node": "^22.20.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.119.0"
  }
}
