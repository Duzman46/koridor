{
  "name": "koridor-rules-tests",
  "description": "Realtime Database security rule tests, run against the Firebase emulator",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "firebase emulators:exec --only database,auth --project koridor-rules-test \"node --test rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1"
  }
}
