{
  "name": "koridor-functions",
  "description": "Server-authoritative rating and leaderboard maintenance for Koridor",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "node --test lib/elo.test.js",
    "test:e2e": "npm run build && firebase emulators:exec --only functions,database --project demo-koridor \"node rating.e2e.js\"",
    "serve": "npm run build && firebase emulators:start --only functions,database",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0",
    "google-auth-library": "^9.14.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
